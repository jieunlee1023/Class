Thread (스레드)
하나의 프로세스는 하나 이상의 thread를 가지게 되고,
실제 작업을 수행하는 단위는 thread다.
하나의 스레드만 있다면 싱글 스레드 프로그래밍
여러개의 스레드가 있다면 멀티 스레드 프로그래밍이라고 함

스레드는 우리가 100% 컨트롤 할 수 없다.

동기화

프로세스
:메모리에 올라가서 동작하고 있는 상태


레퍼클래스


switch문

도메인 XML ( DSL )
어떠한 범주화가 필요하다면 사용하면 된다

열거 타입이란? (enum)
서로 연관된 상수의 집합을 저장하는 자료형

 

데이터 중에는 몇 가지로 한정된 값만을 갖는 경우가 있다. 예들 요일에 대한 데이터(월화수...)는 7개의 값만을 갖고, 계절에 대한 데이터(봄여름...)는 네 개의 값만을 갖는다.

 

이와 같이 한정된 값만을 갖는 데이터 타입이 열거 타입(enumeration type)이다. 